input { stdin { } } 
output { opensearch {
    hosts       => "https://localhost:9200"
    user        => "admin"
    password    => "admin"
    index       => "logstash_test"
    ssl_certificate_verification => false
}}
